[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "reticulumpi"
version = "0.2.0"
description = "An extensible Reticulum network node for Raspberry Pi"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = [
    "rns>=1.1.0,<2.0",
    "lxmf>=0.9.0,<1.0",
    "pyyaml>=6.0",
    "psutil>=5.9",
]

[project.optional-dependencies]
nomadnet = [
    "nomadnet>=0.5,<1.0",
]
dashboard = [
    "aiohttp>=3.9,<4.0",
]
lora = [
    "rnodeconf>=2.0,<3.0",
]
sensors = [
    "smbus2>=0.4,<1.0",
    "RPi.GPIO>=0.7,<1.0",
]
meshtastic = [
    "meshtastic>=2.0,<3.0",
    "paho-mqtt>=2.0,<3.0",
    "cryptography>=3.0,<5.0",
]
meshcore = [
    "meshcore>=2.3,<3.0",
]
dev = [
    "pytest>=7.4,<9.0",
    "pytest-cov",
    "pytest-mock",
    "ruff",
]

[project.scripts]
reticulumpi = "reticulumpi.cli:main"

[tool.setuptools.package-data]
reticulumpi = ["data/*.yaml"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: tests that require a real Reticulum instance",
]
