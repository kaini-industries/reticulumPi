# ReticulumPi 0.3.3 Withdrawal Record

Status: **withdrawn—never approved for stable promotion**

This working-tree record is not a release attestation. It records local implementation evidence
separately from the signed CI, independent audit, performance, accessibility, and hardware gates.
Version 0.3.3 superseded the withdrawn, never-published 0.3.2 candidate, but was itself withdrawn
after production preflight found a legacy-configuration compatibility defect. It will not be moved
or reused.

## Artifact identity

- Signed tag CI and two-round Minisign candidate assembly: completed before production preflight
- Exact-candidate stable promotion: canceled after the production compatibility failure
- Predecessor failure record: [`0.3.2.md`](0.3.2.md)
- Coverage bootstrap baseline: historical 0.2.4 version boundary
  `89249b8b58cb86ac14ff7179abbbca3cb762d2a4`; the withdrawn 0.3.2 tag is deliberately not
  accepted as a completed coverage boundary
- Candidate commit `48e793d27d9389ab2d98ec592041d112cb17c5e5` and strict
  first-parent/version-check evidence: completed by signed-tag CI before withdrawal

## Production qualification failure

- The r2 deployment kit verified the exact signed candidate and captured the immutable predecessor
  baseline before entering the manifest/preflight sequence.
- The dependency-free recovery parser rejected a valid YAML indentationless sequence under
  `reticulumpi.plugins.sensor_framework.sensors` before any administrator transaction began.
- The deployment contingency restored the exact predecessor config bytes and metadata, removed the
  staged external manifest, and verified `reticulumpi.service` remained active.
- No cutover, hardware qualification, soak clock, or stable GitHub Release occurred.

## Stabilization evidence

- Python 3.11–3.14 matrix: pending final CI. The 2026-07-13 local Python 3.14 serial coverage lane
  passed 4,135 tests. Local coverage was 79.01% line and 70.57% branch. Changed executable lines
  were 93.00% covered against the pre-PR main commit and 92.33% covered across 12,104 executable
  lines changed since the pinned 0.2.4 boundary; identity, authentication, lifecycle, migration,
  and installer modules each exceeded 90% line and branch coverage
- No first-party warnings with warnings-as-errors: the local full-tree lane passed; final release
  CI remains authoritative
- Secret-free operational metric inventory, strict CSP, cross-browser smoke, performance,
  accessibility, documentation, and systemd gates: pending signed candidate execution

## 72-hour qualification

The Pi 5/RNode/Meshtastic/RTL-SDR/GPS candidate soak was **not performed** because production
preflight failed first.

## Approval

- Engineering: withdrawn
- Security and final audit: withdrawn pending the 0.3.4 replacement
- Accessibility/design score: not reached
- 72-hour hardware qualification: not reached
- Release manager: stable promotion canceled
