# ReticulumPi 0.3.5 Release Verification

Status: **candidate—not approved for stable promotion**

This working-tree record is not a release attestation. It defines the replacement candidate scope
and keeps implementation evidence separate from signed CI, independent audit, production,
hardware, and soak gates. Version 0.3.5 replaces the withdrawn, never-promoted 0.3.4 candidate.

## Artifact identity

- Candidate commit and signed `v0.3.5` tag: pending
- Signed-tag CI/source run and attested input-manifest digest: pending
- Candidate-finalization/global-signing-request run: pending
- Release workflow run and exact signed candidate artifact: pending
- Wheel, source archive, recovery packages, install archive, container archives, SBOM, provenance,
  SHA-256 manifests, and both Minisign signatures: pending
- Exact-candidate promotion/no-rebuild attestation and published image digest: pending
- Predecessor failure record: [`0.3.4.md`](0.3.4.md)

## Production compatibility correction

The replacement must preserve the distinction between `device_serial` and `device_index` after
configuration loading. A value sourced from `device_index` must remain an index even when its
zero-padded decimal form also matches the lexical shape of an RTL-SDR serial. A value sourced from
`device_serial` must remain a strict serial selection and must not silently fall back to an index.

That selector type must remain bound through plugin initialization, scheduler registration,
device claim, re-enumeration refresh, contention handling, and release. Regression coverage must
exercise both interpretations of the same numeric-looking value, canonical duplicate-claim
protection, scheduler forwarding, and affected plugin paths.

No implementation, CI, artifact, production, or hardware gate is recorded as passed here. The
exact signed 0.3.5 candidate must rerun every applicable gate; 0.3.4 qualification evidence cannot
be relabeled or carried forward.

## Production and hardware qualification

- Exact signed-candidate verification and production preflight: pending
- Transactional apply, automatic contingency, acceptance, and identity/database continuity:
  pending
- Explicit serial/index binding on representative RTL-SDR hardware: pending
- SDR contention, failure/restart, and unplug/replug recovery: pending
- RNode, Meshtastic, GPS, Dashboard, NomadNet, networking, reboot, and container checks: pending

## 72-hour qualification

The complete Pi 5 production soak has not started. The protected GitHub `release` environment must
remain unapproved until the exact signed 0.3.5 candidate completes the hardware sequence and the
full 72-hour clock with a signed qualification record.

## Approval

- Engineering: pending
- Security and final audit: pending
- Accessibility/design score: pending
- 72-hour hardware qualification: pending
- Release manager: pending
