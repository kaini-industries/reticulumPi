# ReticulumPi 0.2.5 Release Verification

Status: **candidate—not approved for stable promotion**

## Artifact evidence

- Exact signed tag/commit: pending.
- Wheel, sdist, and ARM64 bundle names, byte sizes, SHA-256 values, and archive inventories:
  pending the final clean `v0.2.5` candidate build.
- A prior unsigned dirty-worktree build exercised the isolated build, `twine check`, archive
  inspection, source-removed wheel smoke, dashboard assets, and hash-locked install path. Later
  remediation changed the worktree, so its filenames, hashes, member counts, and test counts are
  deliberately not retained as current release evidence. The exact candidate must rerun every
  gate and record its results here.
- Signed-tag trust and exact-version checks, nested/global Minisign manifests, installed-wheel
  verification, SBOM validation, provenance, and no-rebuild publication are implemented in the
  release workflow; successful signed CI evidence remains mandatory.
- ARM64/AMD64 image digests: pending CI
- SBOM/provenance/Minisign: pending release workflow

## Automated checks

- Python 3.11–3.14 matrix and authoritative Bookworm/Python 3.11 lane: pending final CI.
- Independent serial full suite, aggregate/branch coverage, and 90% changed/critical coverage:
  pending a final run after the last remediation change. Earlier exact counts are superseded.
- Installed-wheel dashboard asset and authenticated HTTP smoke with the source tree unavailable:
  implemented and previously exercised on a development wheel; exact-candidate rerun pending.
- Ruff check/format, dashboard build/syntax, shell, YAML, Compose, documentation, and secret
  scan: local results are recorded only after the final worktree gate; CI remains authoritative
- An earlier local exploratory ARM64 image verified fixed UID/GID 10001, non-root execution under
  `tini`, readiness, absence of compiler/source/sudo, graceful TERM in approximately 0.75
  seconds, and persistence of identity, database, NomadNet-content, and dashboard-secret
  hashes across recreation. That image predates later remediation and is not candidate evidence.
  Rebuilding both architectures from the exact signed release artifacts, recording their
  digests, and repeating these checks in CI remain pending.

## Security bridge evidence

- Root-owned control broker and transactional administrator regressions: implemented locally;
  signed CI/Bookworm evidence pending
- Legacy sudo-rule/helper removal and password/session rotation procedure:
  [0.2.5 security advisory](../security-advisory-0.2.5.md)
- Generated bootstrap password retained until durable change: local regression implemented;
  browser/security release gate pending
- NomadNet packaging and container persistence are implemented and verified locally as
  described above; representative-device and multi-architecture evidence remain pending

## Hardware evidence

The Raspberry Pi 5/RNode/Meshtastic/RTL-SDR/GPS qualification and 72-hour soak have not been
performed in this workspace. Stable publication is blocked until a human tester completes
[Hardware Validation](../hardware-validation.md), records hardware revisions, verifies
identity continuity and rollback, and signs this file.

## Approval

- Engineering: pending
- Security: pending
- Hardware qualification: pending
- Release manager: pending
