# ReticulumPi 0.3.4 Release Verification

Status: **candidate—not approved for stable promotion**

This working-tree record is not a release attestation. It records local implementation evidence
separately from signed CI, independent audit, performance, accessibility, and hardware gates.
Version 0.3.4 replaces the withdrawn, never-promoted 0.3.3 candidate.

## Artifact identity

- Signed tag/commit, hashes, image digests, SBOM, provenance, and Minisign: pending
- Exact-candidate promotion/no-rebuild attestation: pending
- Predecessor failure record: [`0.3.3.md`](0.3.3.md)
- Candidate commit and strict tag verification: pending signed tag execution

## Production compatibility correction

- The dependency-free recovery projection accepts an indentationless block sequence only when an
  empty-valued, projection-irrelevant mapping owns it.
- The production-shaped `sensor_framework.sensors` configuration and multiple sequence items are
  covered without projecting nested migration-looking keys.
- Orphan sequences, sequences owned by relevant fields, invalid mapping/sequence mixtures,
  duplicate relevant mappings, and unsupported YAML features still fail closed.
- Local targeted recovery and administrator validation passed 384 tests; final multi-version,
  systemd, artifact reproducibility, and production-shaped CI remain authoritative.

## 72-hour qualification

The Pi 5/RNode/Meshtastic/RTL-SDR/GPS candidate soak is **not yet performed**. Production cutover,
functional hardware records, reboot checkpoint, resource baselines, and the complete 72-hour clock
must pass before stable promotion.

## Approval

- Engineering: pending
- Security and final audit: pending
- Accessibility/design score: pending
- 72-hour hardware qualification: pending
- Release manager: pending
