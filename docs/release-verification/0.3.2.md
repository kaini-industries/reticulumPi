# ReticulumPi 0.3.2 Release Verification

Status: **withdrawn—tag CI failed; this version will not be reused or published**

This record is not a release attestation. It preserves the failed candidate identity and local
implementation evidence separately from successful signed CI, hardware qualification, and stable
promotion evidence.

## Artifact identity

- Signed tag: `v0.3.2`; annotated tag object `c91af007d43942fe1eae80c4e1adb3a53c23a13f`;
  commit `86ff0d54e4b6ea841122b88e2d2a4a1a81a04e8e`; OpenPGP signature verified locally
- Tag CI: [run 29294084651](https://github.com/kaini-industries/reticulumPi/actions/runs/29294084651),
  attempt 1, cancelled after the release-tag trust and signed-version jobs failed
- Hashes, image digests, final SBOM/provenance attestations, Minisign signatures, and exact-candidate
  promotion: not produced
- Coverage bootstrap baseline: historical 0.2.4 version boundary
  `89249b8b58cb86ac14ff7179abbbca3cb762d2a4`. This unsigned, untagged commit is used only
  as an exact changed-code baseline; it is not represented as a historical release attestation
- Candidate commit and strict first-parent/version-check evidence: pending signed tag execution

## Stabilization evidence

The tag run exposed a release-only workflow defect: the implicit `actions/checkout` tag path first
fetched the annotated object and then replaced the local `refs/tags/v0.3.2` with its peeled commit.
Both independent consumers correctly failed closed because the local ref was no longer an annotated
tag. No offline signing request was produced. The run was cancelled after preserving the failed job
logs, and this version is permanently withdrawn; the checkout fix must pass under a new signed tag.

- Python 3.11–3.14 matrix: pending final CI. The 2026-07-13 local Python 3.14 serial coverage lane
  passed 4,135 tests. Local coverage was 79.01% line and 70.57% branch. Changed executable lines
  were 93.00% covered against the pre-PR main commit and 92.33% covered across 12,104 executable
  lines changed since the pinned 0.2.4 boundary; identity, authentication, lifecycle, migration,
  and installer modules each exceeded 90% line and branch coverage
- No first-party warnings with warnings-as-errors: the local full-tree lane passed; final release
  CI remains authoritative
- Secret-free operational metric inventory: fixed-cardinality dashboard coverage is exercised
  by `tests/test_dashboard_operational_metrics.py`; signed CI and Pi soak-series evidence pending
- Strict CSP and cross-browser smoke: local implementation/regression present; independent
  security review and signed release evidence pending
- Complete re-audit with no P0/P1 and dashboard score ≥18/20: pending independent review
- Documentation examples, CLI snapshots, route/default comparison, internal links, and stale
  reference scan: local gates present. Explicit `bookworm-doctest` discovery and a clean-environment
  runner are wired into the systemd-capable Bookworm fixture. The local ARM64 Bookworm/systemd
  execution passed these gates on 2026-07-11; tagged execution and signed publication remain
  pending.

The local throttled Chromium performance lane passed with a 763 ms usable shell, three critical
requests, 752 ms LCP, 24 ms longest interaction, 0.0499 CLS, no long task, and 116.23 measured
frames per second. Signed CI, WebSocket payload budgets, and reference-device performance remain
promotion gates.

## 72-hour qualification

The Pi 5/RNode/Meshtastic/RTL-SDR/GPS candidate soak is **not yet performed**. Record unit
failures, orphan processes/destinations, SQLite integrity, OOM events, thread/descriptor
trends, RSS growth, temperatures, throttling, restarts, and identity continuity here or in a
signed attached record.

## Approval

- Engineering: pending
- Security and final audit: pending
- Accessibility/design score: pending
- 72-hour hardware qualification: pending
- Release manager: withdrawn after failed tag-only gates
