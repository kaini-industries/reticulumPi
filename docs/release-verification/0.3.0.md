# ReticulumPi 0.3.0 Release Verification

Status: **candidate—not approved for stable promotion**

This working-tree record contains no signed release evidence. Local regressions and exploratory
container checks below are implementation evidence only; they do not satisfy a promotion gate.

## Artifact identity

- Signed tag and commit: pending
- Wheel/sdist and ARM64 bundle SHA-256: pending CI
- ARM64/AMD64 image digests: pending CI
- Minisign signature, CycloneDX SBOM, and provenance: pending CI
- setuptools-scm/archive fallback, hash locks, strict tag/artifact checks, and one-wheel Docker
  inputs: implemented locally; tagged CI transcript and trusted signature verification pending

## Automated and transactional evidence

- Python 3.11–3.14, line ≥65%, branch ≥55%, changed/critical ≥90%: pending final CI
- Installed-wheel/source-removed hash-locked smoke and same-wheel multi-architecture builds:
  configured; a local ARM64 recreation/persistence check was completed, while signed CI,
  AMD64, image-scan, and candidate-digest evidence remain pending
- Bookworm fresh/custom-root/idempotent install, injected failure, interrupted recovery, and
  rollback: the ARM64 Bookworm CI fixture now builds ephemeral Minisign-signed candidates and is
  configured to apply/start them under systemd at both `/opt/reticulumpi` and
  `/srv/reticulumpi`. It checks exact-artifact no-op behavior, injects a durable pre-backup
  interruption, rejects a signed candidate whose service cannot start, verifies automatic
  restoration, performs a successful upgrade, and manually rolls back. Local failpoint
  regressions also cover a durable pre-candidate `preparing` journal,
  simulated power loss with recursive partial-release cleanup, platform rejection, installed-unit custom
  layout discovery, signed inner/outer ARM64 archive extraction, hash-locked profile selection,
  user-controlled install-root rejection, manifest/wheel replacement attacks, root-only journal
  survival across state replacement, credential drop-in cleanup, migration-target symlink rejection,
  current/manifest mismatch recovery, missing-backup fail-close, managed-unit/state restoration,
  prior service-state restoration, coordinated `rnsd`, and Dashboard listener readiness. A
  separate ARM64 image gate installs the all-features lock with `--require-hashes`, runs
  `pip check`, and resolves each hardware integration distribution. On 2026-07-11 a local ARM64
  Bookworm/Python 3.11 systemd runs at `/opt/reticulumpi` and `/srv/reticulumpi` completed fresh
  installation, readiness, exact-artifact no-op, interrupted-transaction recovery, and unchanged
  identity. The `/opt` run also completed an intentionally failed signed upgrade with automatic
  rollback, a successful 0.3.1 upgrade, and manual rollback. The root/systemd administrator and
  runtime-layout suite passed 229 tests in each layout. The local all-features ARM64 hash-lock gate
  also passed. These are unsigned working-tree results; tagged CI evidence and hardware validation
  remain promotion gates
- Declarative migration plan/dry-run/apply/backup/restore failpoints: local regressions
  present; signed CI evidence pending
- Lifecycle/resource baseline, callback isolation, process/SDR supervision: focused local
  regressions present; complete signed release-suite and hardware-fixture evidence pending

## Hardware evidence

Pi 5 with RNode, Meshtastic, RTL-SDR, GPS, offline/captive transitions, identity continuity,
readiness/shutdown deadlines, and container recreation: **pending human qualification**.

## Approval

- Engineering: pending
- Security: pending
- Hardware qualification: pending
- Release manager: pending

Promote only these exact signed candidate artifacts; do not rebuild after qualification.
